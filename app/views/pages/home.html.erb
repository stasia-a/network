<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/post_form' %>
      </section>
    </aside>
  </div>  
<% else %>
  <div class="center hero-unit">
    <h1>Welcom</h1>
      <h2>Join us and enjoy!!!!!!!</h2>
 <%= link_to "Sign up", new_user_registration_path, class: "btn btn-large btn-primary" %> </div>
<% end %>
