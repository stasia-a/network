<%= form_for current_user.relationships.build(:friend_id => @user.id), :remote => true do |f| %>
  <div><%= f.hidden_field :friend_id %></div>
  <%= f.submit "Invite new friend!", class: "btn btn-large btn-primary" %>
<% end %>
