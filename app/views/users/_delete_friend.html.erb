<%= form_for current_user.relationships.find_by_friend_id(@user),
             :html => { :method => :delete }, :remote => true do |f| %>
  <%= f.submit "Delete friend", class: "btn btn-large" %>
<% end %>
